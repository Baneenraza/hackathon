# ── Full development requirements (local training + experiment tracking) ──────
# Install this to re-run the whole pipeline in README §8.
# The DEPLOYED app only needs requirements.txt (which uses tensorflow-cpu).
# Do NOT install both this and requirements.txt into the same env — tensorflow
# and tensorflow-cpu conflict.

streamlit>=1.40
numpy>=2.2,<2.6
pandas>=2.2
matplotlib>=3.8
pillow>=10.0
joblib>=1.4
scikit-learn==1.9.0
shap==0.52.0
sentence-transformers>=3.0,<7
faiss-cpu>=1.9
groq>=0.11
fpdf2>=2.8
python-dotenv>=1.0

# training / tracking only
tensorflow==2.21.0
xgboost>=2.1
mlflow>=2.16
opencv-python>=4.9
plotly>=5.20
pypdf>=4.0
kaggle>=1.6
tabulate>=0.9
