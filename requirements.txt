# ── Deployment / runtime requirements (Streamlit Community Cloud, HF Spaces) ──
#
# REQUIRES PYTHON 3.12 (or 3.11). TensorFlow has NO wheels for Python 3.13/3.14 —
# set the Python version to 3.12 in the Streamlit Cloud app settings, or this
# build will fail on tensorflow-cpu. A .python-version file (3.12) is included.
#
# Only three packages are pinned exactly — they must match the committed model
# artifacts (.keras saved with TF 2.21; .joblib / SHAP explainer pickled with
# scikit-learn 1.9 / shap 0.52). Everything else floats.

streamlit
numpy<2.6
pandas<3
matplotlib
pillow
joblib

scikit-learn==1.9.0
shap==0.52.0
tensorflow-cpu==2.21.0

sentence-transformers
faiss-cpu
groq
fpdf2
python-dotenv
